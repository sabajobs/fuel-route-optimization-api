from django.db import models

# No database models are required for this project.
# Fuel station data is stored as a preprocessed JSON dataset and
# loaded directly into memory for fast read-only access.
